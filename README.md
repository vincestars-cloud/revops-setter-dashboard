# RevOps — Appointment Setter Dashboard

Static dashboard (GitHub Pages). Reads live setter metrics from an n8n webhook (set `LIVE_ENDPOINT` in index.html); falls back to representative data until then. Metrics computed per GHL tag/field formulas + Hubstaff hours.
